{
  "name": "shop",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/shop/src",
  "projectType": "application",
  "targets": {
    "deploy": {
      "dependsOn": ["build"],
      "command": "az storage blob upload-batch -s './dist' -d '$web' --account-name 'amakas001' --overwrite"
    }
  },
  "tags": []
}
